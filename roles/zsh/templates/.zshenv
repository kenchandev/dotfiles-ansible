. "$HOME/.cargo/env"

export HOMEBREW_BIN_DIR=/opt/homebrew/bin
export VOLTA_BIN_DIR=${HOME}/.volta/bin
export LIBPQ_BIN_DIR=/opt/homebrew/opt/libpq/bin

export PATH=${PATH}:${HOMEBREW_BIN_DIR}:${VOLTA_BIN_DIR}:${LIBPQ_BIN_DIR}